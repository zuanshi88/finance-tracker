 <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to 'FinanceTracker', root_path, class: 'navbar-brand'%>
   
   <% if current_user %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item"  >
                <% request.path == my_portfolio_path ? port_class = "nav-link active" : port_class = "nav-link" %>
                <%= link_to 'My Portfolio', my_portfolio_path, class: port_class %>
              </li>
              <li class="nav-item">
            <% request.path == my_friends_path ? friend_class = "nav-link active" : friend_class = "nav-link" %>
                <%= link_to 'Friends', my_friends_path, class: friend_class %>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Profile
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <%= link_to edit_user_registration_path, class: "dropdown-item" do %>
                      <%= fa_icon 'edit' %> Edit profile
                  <% end %>
                  <li><a class="dropdown-item" href="#">Another action</a></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item" href="#">Something else here</a></li>
                </ul>
              </li>
              <li class="nav-item">
                <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
              </li>
              </ul>

              <ul class='navbar-nav'>
                  <li class='nav-item user-name'>
                    <%= fa_icon 'user' %>   <%= current_user.full_name %>
                  </li>
                  <li class="nav-item'">
                    <%= link_to("Logout", destroy_user_session_path, class: "btn btn-xs btn-danger logout-button", method: :delete)%>
                  </li>
              </ul>
            <form class="d-flex">
              <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
              <button class="btn btn-outline-success" type="submit">Search</button>
            </form>
          </div>
      <% end %>
  </div>
</nav>